import React, { PropTypes } from 'react'

const propTypes = {}
const defaultProps = {}

const InternalDisplacement = () => {
  return <div>InternalDisplacement</div>
}

InternalDisplacement.propTypes    = propTypes
InternalDisplacement.defaultProps = defaultProps

export default InternalDisplacement
